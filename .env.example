# ===========================================
#  在问AI API 配置文件
#  使用方法: cp .env.example .env  然后编辑 .env
# ===========================================

# 服务端口 (确保不与服务器已有项目冲突)
PORT=3456

# Token 列表，多个用英文逗号分隔
TOKENS=你的token1,你的token2

# 最低余额，低于此值自动跳过该token
MIN_BALANCE=100
