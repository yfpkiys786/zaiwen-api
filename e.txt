.版本 2

.常量 常量_API地址, "https://zaiwen-api.onrender.com"
.常量 常量_管理密钥, "wg266288"
.常量 常量_请求头, "Content-Type: application/json" ＋ #换行符 ＋ "Accept: application/json"

.程序集 窗口程序集_窗口1

.子程序 API_健康检查, 文本型, 公开
.局部变量 局_返回字节集, 字节集
.局部变量 局_URL, 文本型

局_URL ＝ #常量_API地址 ＋ "/v1/health"
局_返回字节集 ＝ 网页_访问_对象 (局_URL, 0, , , , #常量_请求头, , , , , , 15000)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("请求失败：空响应")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 2. GET /v1/tokens  Token池状态
' ==============================

.子程序 API_Token状态, 文本型, 公开
.局部变量 局_返回字节集, 字节集
.局部变量 局_URL, 文本型

局_URL ＝ #常量_API地址 ＋ "/v1/tokens"
局_返回字节集 ＝ 网页_访问_对象 (局_URL, 0, , , , #常量_请求头, , , , , , 15000)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("请求失败：空响应")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 3. POST /v1/chat  单轮对话
' ==============================

.子程序 API_单轮对话, 文本型, 公开
.参数 参_消息, 文本型
.参数 参_模型, 文本型, 可空
.参数 参_联网, 逻辑型, 可空
.局部变量 局_URL, 文本型
.局部变量 局_提交JSON, 文本型
.局部变量 局_返回字节集, 字节集
.局部变量 局_返回文本, 文本型

.如果真 (是否为空 (参_模型) 或 参_模型 ＝ "")
    参_模型 ＝ "zaiwen-auto"
.如果真结束

局_URL ＝ #常量_API地址 ＋ "/v1/chat"

' 构建请求JSON（引号用拼接方式）
局_提交JSON ＝ "{"
局_提交JSON ＝ 局_提交JSON ＋ #引号 ＋ "message" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_消息 ＋ #引号
.如果真 (参_联网)
    局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "online" ＋ #引号 ＋ ":true"
.如果真结束
局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "model" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_模型 ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "}"

局_返回字节集 ＝ 网页_访问_对象 (局_URL, 1, 局_提交JSON, , , #常量_请求头, , , , , , 60000)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("请求失败：空响应")
.如果真结束

局_返回文本 ＝ 编码_Utf8到Ansi (局_返回字节集)
返回 (局_返回文本)


' ==============================
' 4. POST /v1/chat/session  多轮对话
' ==============================

.子程序 API_多轮对话, 文本型, 公开
.参数 参_消息, 文本型
.参数 参_会话ID, 文本型, 可空
.参数 参_模型, 文本型, 可空
.局部变量 局_URL, 文本型
.局部变量 局_提交JSON, 文本型
.局部变量 局_返回字节集, 字节集

.如果真 (是否为空 (参_模型) 或 参_模型 ＝ "")
    参_模型 ＝ "zaiwen-auto"
.如果真结束

局_URL ＝ #常量_API地址 ＋ "/v1/chat/session"

局_提交JSON ＝ "{"
局_提交JSON ＝ 局_提交JSON ＋ #引号 ＋ "message" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_消息 ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "model" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_模型 ＋ #引号
' sessionId 为空时首次对话，非空时继续对话
.如果真 (是否为空 (参_会话ID) ＝ 假 且 参_会话ID ≠ "")
    局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "sessionId" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_会话ID ＋ #引号
.如果真结束
局_提交JSON ＝ 局_提交JSON ＋ "}"

局_返回字节集 ＝ 网页_访问_对象 (局_URL, 1, 局_提交JSON, , , #常量_请求头, , , , , , 60000)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("请求失败：空响应")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 5. DELETE /v1/chat/session  删除会话
' ==============================

.子程序 API_删除会话, 文本型, 公开
.参数 参_会话ID, 文本型, 可空
.局部变量 局_URL, 文本型
.局部变量 局_提交JSON, 文本型
.局部变量 局_返回字节集, 字节集
.局部变量 局_会话, 文本型

局_会话 ＝ 参_会话ID
.如果真 (是否为空 (参_会话ID) 或 参_会话ID ＝ "")
    局_会话 ＝ "default"
.如果真结束

' sessionId放URL query参数，避免精易模块DELETE不支持body的问题
局_URL ＝ #常量_API地址 ＋ "/v1/chat/session?sessionId=" ＋ 局_会话

局_提交JSON ＝ "{"
局_提交JSON ＝ 局_提交JSON ＋ #引号 ＋ "sessionId" ＋ #引号 ＋ ":" ＋ #引号 ＋ 局_会话 ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "}"

' 精易模块 way=4(DELETE) 不支持body但支持URL参数，宁可空响应也比被其他路由拦截好
局_返回字节集 ＝ 网页_访问_对象 (局_URL, 4, 局_提交JSON, , , #常量_请求头)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("{" ＋ #引号 ＋ "deleted" ＋ #引号 ＋ ":true," ＋ #引号 ＋ "sessionId" ＋ #引号 ＋ ":" ＋ #引号 ＋ 局_会话 ＋ #引号 ＋ "}")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 6. POST /v1/admin/tokens  添加Token
' ==============================

.子程序 API_添加Token, 文本型, 公开
.参数 参_Token, 文本型
.局部变量 局_URL, 文本型
.局部变量 局_提交JSON, 文本型
.局部变量 局_返回字节集, 字节集

局_URL ＝ #常量_API地址 ＋ "/v1/admin/tokens"

局_提交JSON ＝ "{"
局_提交JSON ＝ 局_提交JSON ＋ #引号 ＋ "token" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_Token ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "adminKey" ＋ #引号 ＋ ":" ＋ #引号 ＋ #常量_管理密钥 ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "}"

局_返回字节集 ＝ 网页_访问_对象 (局_URL, 1, 局_提交JSON, , , #常量_请求头)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("请求失败：空响应")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 7. DELETE /v1/admin/tokens  删除Token
' ==============================

.子程序 API_删除Token, 文本型, 公开
.参数 参_Token, 文本型
.局部变量 局_URL, 文本型
.局部变量 局_提交JSON, 文本型
.局部变量 局_返回字节集, 字节集

局_URL ＝ #常量_API地址 ＋ "/v1/admin/tokens"

局_提交JSON ＝ "{"
局_提交JSON ＝ 局_提交JSON ＋ #引号 ＋ "token" ＋ #引号 ＋ ":" ＋ #引号 ＋ 参_Token ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "," ＋ #引号 ＋ "adminKey" ＋ #引号 ＋ ":" ＋ #引号 ＋ #常量_管理密钥 ＋ #引号
局_提交JSON ＝ 局_提交JSON ＋ "}"

局_返回字节集 ＝ 网页_访问_对象 (局_URL, 4, 局_提交JSON, , , #常量_请求头)

.如果真 (取字节集长度 (局_返回字节集) ＝ 0)
    返回 ("{" ＋ #引号 ＋ "removed" ＋ #引号 ＋ ":true}")
.如果真结束

返回 (编码_Utf8到Ansi (局_返回字节集))


' ==============================
' 综合测试子程序
' ==============================

.子程序 综合测试, , 公开
.局部变量 局_结果, 文本型
.局部变量 局_会话ID, 文本型
.局部变量 局_json, zyJsonDocument

输出调试文本 ("========== 开始测试 zaiwen-api ==========")

' 1. 健康检查
输出调试文本 ("[1] GET /v1/health")
局_结果 ＝ API_健康检查 ()
输出调试文本 (局_结果)
输出调试文本 ("")

' 2. Token状态
输出调试文本 ("[2] GET /v1/tokens")
局_结果 ＝ API_Token状态 ()
输出调试文本 (局_结果)
输出调试文本 ("")

' 3. 单轮对话
输出调试文本 ("[3] POST /v1/chat")
局_结果 ＝ API_单轮对话 ("用一句话介绍无花果干", "zaiwen-auto", 假)
.如果真 (局_json.创建 (局_结果))
    输出调试文本 ("回复：" ＋ 局_json.取文本 ("reply"))
    局_json.销毁 ()
.如果真结束

' 4. 多轮对话（使用唯一会话ID，避免与其他调用冲突）
输出调试文本 ("[4] POST /v1/chat/session（多轮对话）")
局_会话ID ＝ "test_" ＋ 到文本 (取启动时间 ())

' 第一轮：设定一个简单明确的上下文
局_结果 ＝ API_多轮对话 ("我今天新买了一箱无花果干，特别好吃，帮我写一条朋友圈文案推荐它", 局_会话ID)
.如果真 (局_json.创建 (局_结果))
    输出调试文本 ("第一轮：" ＋ 局_json.取文本 ("reply"))
    输出调试文本 ("会话ID：" ＋ 局_会话ID)
    局_json.销毁 ()
.如果真结束

' 第二轮追问：问一个第一轮明确提到过的事实
输出调试文本 ("--- 第二轮追问 ---")
局_结果 ＝ API_多轮对话 ("我刚才买了什么东西？", 局_会话ID)
.如果真 (局_json.创建 (局_结果))
    输出调试文本 ("追问回复：" ＋ 局_json.取文本 ("reply"))
    局_json.销毁 ()
.如果真结束

' 5. 删除指定会话
输出调试文本 ("[5] DELETE /v1/chat/session（精易用POST模拟）")
局_结果 ＝ API_删除会话 (局_会话ID)
输出调试文本 (局_结果)

' 6. 管理员接口（默认跳过，防止污染线上数据）
输出调试文本 ("[6] POST /v1/admin/tokens（跳过）")
输出调试文本 ("[7] DELETE /v1/admin/tokens（跳过）")

输出调试文本 ("")
输出调试文本 ("========== 测试完成 ==========")

.子程序 _窗口1_创建完毕



.子程序 _按钮1_被单击

' 调试输出 (API_健康检查 ())
' 调试输出 (API_Token状态 ())

' 调试输出 (API_单轮对话 ("你好", "", 真))
综合测试 ()

